<h2 class="page-header">Edit location</h2>
<form action="/locations/<%= location.id %>?_method=PUT" method="POST">
  <%- include('./_form_fields.ejs') %>
  <div class="form-row form-row--end row">
    <a class="btn btn-secondary" href="/locations/<%= location.id %>">Cancel</a>
    <button class="btn btn-primary" type="submit">Update</button>
  </div>
</form>